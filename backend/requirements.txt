# KrishiMitra AI — Backend Dependencies
# Pin major versions; allow minor/patch updates for security fixes.

# --- Web framework ---
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# --- Data validation ---
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# --- Environment variable loading ---
python-dotenv>=1.0.0,<2.0.0

# --- IBM watsonx.ai SDK (Granite LLM + Embeddings) ---
# This is the ONLY allowed IBM Cloud SDK in this project.
# Do NOT add openai, anthropic, or other LLM SDKs.
ibm-watsonx-ai>=1.0.0,<2.0.0

# --- Vector store ---
chromadb>=0.5.0,<1.0.0

# --- Text splitting for RAG chunking ---
langchain-text-splitters>=0.2.0,<1.0.0

# --- PDF document loading ---
pypdf>=4.2.0,<5.0.0

# --- Multipart form support (for future file upload endpoints) ---
python-multipart>=0.0.9,<1.0.0

# --- Async HTTP client (used internally by watsonx SDK) ---
httpx>=0.27.0,<1.0.0

# --- Testing ---
pytest>=8.2.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
